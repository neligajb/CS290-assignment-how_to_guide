<?php

function page3() { ?>
  <h2>Creating a node.JS application</h2>
  <p>After creating your project in WebStorm, you can right-click the project in the the project browser and
  select New -> File. It is common practice to use the name 'app.js'.</p>
  <p><img src="files/ws-add-file.png" width="600" ></p>
  <p><img src="files/ws-name-file.png" width="600" ></p>
  <p><br>Now, you have what is essentially a standard JavaScript file, and the application can be run by
  right-clicking in the window, and selecting Run 'app.js'. This will open a console which shows your app's output.</p>
  <p><img src="files/ws-run.png" width="600" ></p>
  <p>In order to enable WebStorm's auto-complete support for Node's global variables and functions, you will need to
  right-click in the window, select Use JavaScript Library, and select Node.js Globals.</p>
  <p><img src="files/ws-node-globals.png" width="600" ></p>
  <p><br>Next, we'll take a look at one of Node's most important features,
    <em><a href="?page=4" title="page 4">modules.</a></em></p>

<?php } ?>